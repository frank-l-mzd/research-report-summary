# 坑

自检发现问题时对照这里。每条都是实拍踩出来的，不是推测。

## 读取

**154 页的报告不能直接丢进 Read。** 那份 Anthropic 威胁情报报告提取出来是 42 万字符，一次性读进去上下文直接报废，而且真正有用的章节被淹掉。先落盘成 txt，再用 `grep` 定位章节标题，按章节分批读。触发线：**>500KB 或 >30 页，一律先落盘**；>100 页必须先定位章节再读。

**Read 会把超长行截断。** 单行超过 2000 字符的部分会被丢掉。PDF 提取常见的超长行是表格和页眉页脚粘连——正好是数据密集的地方。读之前先看有没有超长行，必要时用 `fold` 或按分隔符切开。

**pdfplumber 的告警会混进正文。** 它会往 stderr 刷 `FontBBox` 一类的字体告警，和提取结果搅在一起。管道里加 `grep -v FontBBox`，或者把 stderr 重定向掉。

**pdfplumber 不一定是装好的。** 环境里可能只有裸 Python。先 `python -c "import pdfplumber"` 探一下，没有就建 venv 装，别默认它存在。

## 抓取

**网页文章用抓取工具会被"总结"掉。** 不指定要求的话，拿回来的是模型压缩后的版本，细节全没了——而后续聚类全靠细节。prompt 里必须写死：**"不要总结，尽可能完整保留原文文字"**。拿到压缩稿等于从源头就丢了信息。

**公众号 / 付费墙页面抓不全是常态。** 抓完先看长度是否合理，明显偏短就说明被截断了，要换路径或让用户提供原文。

## 判断

**单方报告的可信度会被系统性高估。** 公司或机构自己发的报告——威胁情报、行业白皮书、年度趋势判断——样本往往是**刻意挑出来的最极端案例**，报告自己也常注明"不代表普遍性"。必须做三件事：标注单方来源、保留原文的置信度标注（有些案例原文就写了"低/中"）、样本选择偏差要单独说明。**别把报告的自述当成事实。**

**点名指控不能照抄。** 报告点名具体国家、公司、个人时（比如某报告点名多家实验室做模型蒸馏），这是**单方归因**，必须标"需第三方交叉验证"，不能放进事实层。这类内容一旦被下游当成结论引用，污染会扩散。

**"验证通过"不等于"订单落地"。** 业绩会里管理层说"测试已经通过了"，和"已经拿到订单"是两件事。两者混在一起，估值就提前透支了。凡是从"已发生"到"将发生"的跳跃，都要显式标出来是推断。

**管理层的乐观判断自带立场。** 他们说"本轮周期上行到明年下半年"，和他自己承认"成熟制程已经过剩"，这两句之间是有张力的。要指出张力，而不是挑一句顺手的用。

## 输出

**只有积极信号，或只有风险关注，都是不合格。** 这是这个技能最容易被违反的一条——顺着材料的语气走很自然，尤其是材料本身有立场的时候。写完先数一下两边各有几条，少一边就补齐。

**口语稿要归并，不要逐句删。** 录音稿里大量重复和填充词，压过头会把承重的判断一起删掉。正确做法是**按主题归并同类项**再抽象，而不是从头到尾做摘要。

**别输出占位符。** "此处待补充"、"（略）"这种一律不许出现。拿不到的信息要么写明"原文未披露"，要么不写。

**默认简体中文。** 用户明确要英文再切，不要因为材料是英文就顺手输出英文。

## 流程

**技能里残留的构建期指令要清掉。** 用 skill-creator 初始化出来的 SKILL.md 会带一段脚手架说明（比如"清除非必要资源"），那是给构建过程看的，不该留在成品里。定稿前通读一遍，把不属于技能行为的段落删干净。

**改技能本体后要同步已安装副本。** 仓库里的 `SKILL.md` 和 `~/.workbuddy/skills/`（或 `~/.claude/skills/`）下那份是两份文件。只改一边，下次触发用的还是旧的。改完立刻复制过去。
